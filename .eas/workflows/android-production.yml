name: Android Production Build

on:
  workflow_dispatch: {}
  # push:
  #   branches: ['main']

jobs:
  build_android_production:
    name: Build Android Production APK
    type: build
    params:
      platform: android
      profile: production
