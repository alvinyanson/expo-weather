[REFACTOR]: Refactor layouts, overlay behaviors, navigation, and theme colors

- Refactor HomeScreen layout on mobile to wrap inside a vertical ScrollView, preventing elements from clipping on smaller screens.
- Refactor SettingsScreen and LoginScreen layouts to wrap contents in ScrollViews for keyboard and screen-height overflow safety.
- Re-position SearchHeader dropdown as an absolute overlay and add a tap-outside-to-dismiss backdrop to prevent layout shift.
- Replace ES2023 .toSorted() with everywhere-compatible .sort() in firestore service to resolve crash on physical devices.
- Make SavedLocationItem components pressable and implement navigation to DetailsScreen on tap, updating unit tests in saved.test.tsx.
- Correct overlay background color in theme configuration to match new deep blue theme.
- Fix gap between WeatherSummaryCard and DailyForecastList on mobile by removing flex: 1 layout on top card.
