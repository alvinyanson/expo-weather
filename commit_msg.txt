[FEAT]: Implement English and Japanese localization for Android platform
- Installed expo-localization and i18n-js dependencies
- Created a centralized i18n service in src/services/i18n.ts with translations for English and Japanese
- Configured app.json with android-specific supportedLocales for per-app language settings
- Modified date/time formatters in utils/formatters.ts to dynamically read the system locale using Hermes Intl API
- Localized weather condition names and descriptions in utils/weatherMapper.ts
- Extracted and localized all hardcoded user-facing strings, buttons, toast messages, and descriptions across screens and components
- Added a language selection setting in SettingsScreen with state management in useSettingsStore
- Configured the root layout RootApp with a dynamic language-bound key to force instant re-render of the entire tree when language is changed
- Mocked expo-localization and configured global __DEV__ environment variable in vitest setup.ts to pass all unit tests
